{
  "name": "phenixflow",
  "version": "0.1.0",
  "private": true,
  "description": "Backend service scaffold for Project phoenixflow",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:sqlite": "vitest run test/sqlite-harness.test.js",
    "lint": "eslint .",
    "verify": "npm run lint && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "better-sqlite3": "^12.6.2",
    "eslint": "^9.20.1",
    "globals": "^15.15.0",
    "supertest": "^7.0.0",
    "vitest": "^3.0.5"
  }
}
